apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service.name }}
  labels:
    app: apache
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: 80
      nodePort: {{ .Version.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: apache
