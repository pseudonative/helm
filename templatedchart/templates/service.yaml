apiVersion: v1
kind: Service
metadata:
  name: {{ include "templatedchart.fullname" . }}
  labels:
    {{- include "templatedchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "templatedchart.selectorLabels" . | nindent 4 }}
